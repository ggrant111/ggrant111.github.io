chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
    // Handle message.
    // For example, based on message type or action:
    // if (message.action === "doSomething") {
    //     // Perform action
    // }
  
    // Optionally send a response
    // sendResponse({status: "received"});
  });
  